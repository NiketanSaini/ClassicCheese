# Copy to .env. These match the local LiveKit container started by
# docker-compose.yml at the project root (--dev-mode key pair), so this
# works out of the box for local dev. Use real, secret values in any
# non-local environment.

LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=864e122d8552f2fe61b377de1f1ab6e5c99a987ed106c984

HTTP_PORT=8090
CORS_ALLOW_ORIGIN=http://localhost:5173
